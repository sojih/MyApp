@*
    lazor 파일 -> .cshtml
    확장자(.cshtml)에서 볼 수 있듯이 C# 코드와 html 코드가 함께 렌더링 되는 view 파일
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<!-- @@ : html에서 C#코드를 쓸수 있게하는 접두사
    asp-validation-for 뒤에 오는 Name, Age, Country가 어디서 오는지 모르기때문에 에러가 발생
       => using을 사용해서 Models 폴더를 참고하게 하고 model중 Student를 사용한다고 표기 -->
@using MyApp.Models
@model Student

<link rel="stylesheet" href="https:maxcdn.bootstrapcdn.com/bootstrap/3.3.7/ccs/bootstrap.min.ccs" />
@* bootstrap이 작동하지 않아 검색하여 주소 반영 *@
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@3.3.7/dist/css/bootstrap.min.css" />
<h3>Add New Student</h3>
<!--
<form class="form-check form-horizontal" method="POST">
    토큰 없이 post 방식으로 값을 보내기 -> asp-antiforgery="false" 추가
-->
<form class="form-check form-horizontal" asp-antiforgery="false" method="POST">
    <div class="form-group">
        <img src="https://image.ibb.co/iKfYKx/computer.jpg" />
    </div>
    <div class="form-group">
        <div class="col-md-3">
            <label>Name:</label>
            @*<input type="text" class="form-control" name="name" />*@
            <!-- name대신 asp-for 속성을 사용하면 name과 id 속성들을 자동적으로 만들어줌-->
            <input type="text" class="form-control" asp-for="Name" />
            <!-- asp-validation-for : validation을 Name 필드에서 사용하겠다, View 폴더안에 imports 파일 추가, 코드 추가 => 색상이 바뀜(인식됨)-->
            <span asp-validation-for="Name"></span>
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-3">
            <label>Age:</label>
            <input type="text" class="form-control" asp-for="Age" />
            <span asp-validation-for="Age"></span>
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-3">
            <label>Country:</label>
            <input type="text" class="form-control" asp-for="Country" />
            <span asp-validation-for="Country"></span>
        </div>
    </div>
    <button type="submit" class="btn btn-default">Submit</button>
</form>
